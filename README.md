# AGNP_submission_commtr
This is the repository for AGNP model. Due to data value and sensitivity, it only contains partial dataset which is for testing. We also provide state_dict for trained models.
